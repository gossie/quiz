import React from 'react';
import { render, cleanup } from '@testing-library/react';
import App from './App';

beforeEach(() => () => cleanup()); 
afterEach(() => cleanup());

test('needs to be implemented', () => {
});
